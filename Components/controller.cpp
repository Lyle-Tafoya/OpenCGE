#include "controller.h"

namespace OpenCGE
{
  // React to an input event encoded in a 'message'
  void Recv(message const& msg)
  {
    // TODO handle input events in a generic way TODO
    
  }

  // Load a keymap for this controller
  void Controller::LoadKeymap(string const& filename)
  {
  }
}
